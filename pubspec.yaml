name: eduvance
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.19.0
  email_otp: ^2.1.0
  mailer: ^6.6.0
  cloud_firestore: ^4.17.5
  cloud_functions: ^4.6.0
  # Required for the Study Schedule Date Formatting
  intl: ^0.20.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/Icon/